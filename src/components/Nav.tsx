import React from 'react';
import { Logo, NavLinks, Search } from './';

export const Nav = () => {
    return(
        <nav>
            <Logo />
            <NavLinks />
            <Search />
        </nav>
    )
}